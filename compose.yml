# Copyright (c) 2026 eele14. All Rights Reserved.
services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
        VITE_FILE_ROOT: ${VITE_FILE_ROOT}
    ports:
      - "80:80"
    restart: unless-stopped
